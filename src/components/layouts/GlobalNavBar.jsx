const GlobalNavBar = () => (
  <>
    <nav className={"global-nav-bar sticky h-14 shadow-md flex justify-between items-center"}>
        <div>Home</div>
        <div>Menu</div>
    </nav>
  </>
);

export default GlobalNavBar;
